package app;

public class Solution {
    public int findPeakElement(int[] nums) {
        int left = 0,
            right = nums.length -1,
            mid = (left + right) /2;
        
        // while (true) {}
        return 0;
    }
}