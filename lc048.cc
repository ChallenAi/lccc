#include <iostream>
#include <vector>

using namespace std;

class Solution {
    vector<int> temp;
private:
    void rotateLine(int x, int y) {}
public:
    void rotate(vector<vector<int>>& matrix) {
        int x = 0;
        int y = 0;
        while (true) {}
        
    }
};